<% if @item.site_menu_links.count > 0 %>
<div class="portfolio_header_menu">
  <%= link_to 'Home', show_portfolio_path(@item.key), class: 'active' %>
  <% @item.site_menu_links.each do |link| %>
    <%= link_to link.name, link.url %>
  <% end %>
</div>
<% end %>
